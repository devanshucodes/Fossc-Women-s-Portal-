#include <bits/stdc++.h>
using namespace std;

int main() {
	int t;
	cin >> t;
	while (t--) {
		int n, x, mult;
		cin >> n >> x;
		mult = n * x;
		if (mult % 4 == 0) {
			cout << mult / 4 << endl;
		}

		else {
			cout << mult / 4 + 1 << endl;
		}

	}
}
